const { prefix } = require("../../config.json");

module.exports = {
	name: "drop",
	aliases: ["d"],
	description: `Drop a card | Usage: ${prefix}drop`,
	enabled: true,
	execute(client, message, args) {
		//
	},
};
